# pnpm strict hoisting — prevents phantom dependency bugs
shamefully-hoist=false
auto-install-peers=true
strict-peer-dependencies=false
node-linker=hoisted
# Vercel needs hoisted for monorepo resolution
public-hoist-pattern[]=*
